const tokens = {
  SESSION_TOKEN: 'cust' // Current User Session Token
}

const limits = {
  USERNAME_MAX_LIMIT: 50,
  USERNAME_MIN_LIMIT: 4,
  PASSWORD_MIN_LIMIT: 6,
}

export default {
  limits,
  tokens,
}